import { colors } from "./colors";
import { lineHeights, space } from "./spacing";
import { fontSizes, fontWeights } from "./fonts";

const theme = { colors, lineHeights, space, fontWeights, fontSizes };

export default theme;
